# TypeScript Config for [Tweed](https://www.npmjs.com/package/tweed)

```shell
> npm install --dev tweed-typescript-config
> echo '{ "extends": "./node_modules/tweed-typescript-config/config.json" }' > tsconfig.json
```
